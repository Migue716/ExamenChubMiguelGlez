# Base del API (sin barra final). Ejemplo IIS Express / Visual Studio:
VITE_API_URL=https://localhost:44309

# Si no defines VITE_API_URL, las peticiones van a rutas relativas /api y Vite
# las reenvía al target configurado en vite.config.ts (mismo host/puerto por defecto).
